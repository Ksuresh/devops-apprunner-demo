version: 1.0
runtime: python3.11

build:
  commands:
    build:
      - pip3 install --upgrade pip
      - pip3 install -r requirements.txt

run:
  command: gunicorn -b 0.0.0.0:8080 app:app
  network:
    port: 8080
